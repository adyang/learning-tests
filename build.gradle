apply plugin: 'java'

repositories {
    jcenter()
    flatDir {
        dirs 'test-libs'
    }
}

dependencies {
	testCompile 'com.google.jimfs:jimfs:1.1'
    testCompile 'junit:junit:4.12'
    testCompile name: 'jar-resource-test'
}
