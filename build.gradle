apply plugin: 'java'

repositories {
    jcenter()
    flatDir {
        dirs 'test-libs'
    }
}

dependencies {
	testImplementation 'com.google.jimfs:jimfs:1.1'
    testImplementation 'junit:junit:4.12'
    testImplementation name: 'jar-resource-test'
}
